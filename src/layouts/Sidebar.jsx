import React from 'react'
import { Menu } from 'antd'

const Sidebar = () => {
  return (
    <Menu
      mode="inline"
      style={{ height: '100%', borderRight: 0 }}
      items={[]}
    />
  )
}

export default Sidebar
