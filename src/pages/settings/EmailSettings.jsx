import React from 'react'
import { Typography, Card } from 'antd'

const { Title } = Typography

const EmailSettings = () => {
  return (
    <div>
      <Title level={2}>Email Settings</Title>
      <Card>
        <p>Email Settings page - Coming soon...</p>
      </Card>
    </div>
  )
}

export default EmailSettings
