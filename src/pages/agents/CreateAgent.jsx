import React from 'react'
import { Typography, Card } from 'antd'

const { Title } = Typography

const CreateAgent = () => {
  return (
    <div>
      <Title level={2}>Create Agent</Title>
      <Card>
        <p>Create Agent page - Coming soon...</p>
      </Card>
    </div>
  )
}

export default CreateAgent
